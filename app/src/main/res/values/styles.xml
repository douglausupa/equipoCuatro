<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Estilo para el título de la Toolbar -->
    <style name="toolbarTitle" parent="TextAppearance.MaterialComponents.Headline6">
    <item name="android:textStyle">bold</item>
    <item name="android:textSize">28sp</item>
    <item name="android:textColor">@color/white</item>
</style>


    <style name="buttonEditar" >
        <item name="backgroundTint">@color/buttonBackgroundColor</item>

        <!-- Bordes redondeados -->
        <item name="shapeAppearance">@style/buttonStyleShape</item>

        <!-- Color del texto -->
        <item name="android:textColor">@color/white</item>
    </style>


    <style name="buttonStyle" parent="Widget.MaterialComponents.Button">
        <!-- Color de fondo -->
        <item name="backgroundTint">@color/buttonBackgroundColor</item>

        <!-- Bordes redondeados -->
        <item name="shapeAppearance">@style/buttonStyleShape</item>

        <!-- Color del texto -->
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="buttonStyleShape" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <style name="buttonStyleGuardar" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/buttonBackgroundColor</item>
        <item name="shapeAppearance">@style/buttonStyleShape</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/edittext_color_states</item>
        <!-- Puedes añadir más personalización aquí -->
    </style>
</resources>